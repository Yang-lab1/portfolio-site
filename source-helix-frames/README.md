# Hero Helix Frame Source

把用户确认的螺旋阶梯帧图放在这个目录。

要求：
- 文件名按顺序排列，例如 `01.png` 到 `25.png`，或任何能按数字自然排序的名称。
- 只放最终确认的白色螺旋阶梯图片，不放聊天截图、网页截图或其它参考图。
- 推荐尺寸一致，最好接近 16:9。

生成命令：

```bash
cmd /c npm run render:helix
```

生成结果：
- `public/hero-yang-helix-loop.mp4`

